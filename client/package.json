{
  "name": "client",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "parcel serve src/index.html --port 8089 --out-dir bld",
    "build": "parcel build src/index.html --out-dir bld",
    "lint": "tslint -c ./tslint.json -p ./tsconfig.json"
  },
  "dependencies": {
    "phaser": "^3.15.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.10.3"
  }
}
